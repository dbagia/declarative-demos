describe('compareTriplets', () => {
    describe('inputs to compareTriplets are number lists ranging from -100, 0 to 100', () => {
        it('is always true', () => {
            expect(true).toBe(true)
        })
    })
    describe('output is a number list of two values > 0', () => { })

})